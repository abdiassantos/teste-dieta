<div class="container space-between-div">
    <h3 class="">
        <strong>
            <%= label_tag "Olá, #{current_user.name}" %>
        </strong>
    </h3>

    <h4>
        <%= link_to "< Registrar peso", new_weight_path, class:"absolute" %>
    <h4>
</div>

<%= area_chart @weights.pluck(:created_at, :peso_atual), xtitle: "Data de registro", ytitle: "Peso",
                            download: {filename: "RegistroDePeso", background: "#fffFFF"} %>

<div class="container space-top">
    <h4 class="">
        <strong>
            <%= label_tag "#{current_user.name}, continue assim, está indo muito bem!" %>
        </strong>
    </h4>
</div>